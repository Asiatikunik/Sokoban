#include "affiche.h"
#include <stdlib.h>

int main() {
	lire_fichier(1);
    initialiser_affichage();
    afficher_jeu();
    //ecrire_texte_dans_cercle("Bonjour le monde !");
    //blackstar();
    exit(0);
}
